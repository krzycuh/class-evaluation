# Obraz z gotowych assetów zbudowanych na runnerze CI (npm run build poza Dockerem).
FROM nginx:1.27-alpine

COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
